import React from 'react';

export default function BookingHistory() {
	// Placeholder for booking history
		return (
			<div className="text-gray-600 text-sm w-full max-w-full">No bookings yet.</div>
		);
}
